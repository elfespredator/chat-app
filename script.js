// Optionnel : Ajout de commandes pour manipuler la musique
document.addEventListener('DOMContentLoaded', () => {
    const audio = document.querySelector('audio');
    audio.volume = 0.5; // Réglez le volume
});
